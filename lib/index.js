// Host half: no-op. This is a pure client plugin — the browser half
// (lib/client.js) registers the Slot UI; the node half contributes nothing.
export const name = "dsh-thinking-fullscreen";

export function apply() {}
